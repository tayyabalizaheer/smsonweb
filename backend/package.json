{
  "name": "sms-one-way-sync",
  "version": "1.0.0",
  "description": "Lightweight one-way SMS synchronization backend using Node.js, Express, EJS, and MySQL.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/config/db.js && node --check src/routes/api.js && node --check src/routes/web.js"
  },
  "keywords": [
    "sms",
    "express",
    "mysql",
    "ejs",
    "android"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.7",
    "ejs": "^3.1.10",
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "mysql2": "^3.11.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "engines": {
    "node": ">=18"
  }
}
